{
  "name": "@vysion/database",
  "version": "1.0.0",
  "description": "Centralized Prisma database configuration for Vysion Analytics",
  "main": "index.js",
  "scripts": {
    "migrate": "npx prisma migrate dev",
    "migrate:deploy": "npx prisma migrate deploy",
    "migrate:reset": "npx prisma migrate reset",
    "generate": "npx prisma generate",
    "studio": "npx prisma studio",
    "seed": "npx ts-node prisma/seed.ts",
    "db:push": "npx prisma db push",
    "db:pull": "npx prisma db pull"
  },
  "keywords": [
    "prisma",
    "database",
    "postgresql"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prisma": "^6.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
