import { Text, View } from 'react-native';

export default function ScoutPlayerDetailScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Scout Player Detail Screen</Text>
    </View>
  );
}
